# Modular demo
Input and output validation run in the main flow. An injected provider owns network
timeouts, bounded retries, safe fallback and operational logging. Configuration is
external and validated. No source in this fixture is executed by the assessor.
